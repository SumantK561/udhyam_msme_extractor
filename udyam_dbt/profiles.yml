# Used by CI (dbt parse) only — no real connection is made.
# Production: use ~/.dbt/profiles.yml with real credentials.
udyam_dbt:
  target: ci
  outputs:
    ci:
      type: snowflake
      account: dummy
      user: dummy
      password: dummy
      role: dummy
      warehouse: dummy
      database: UDYAM
      schema: RAW
      threads: 1
